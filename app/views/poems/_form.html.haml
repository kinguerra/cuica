= form_for @poem do |f|
  - if @poem.errors.any?
    #error_explanation
      %h2= "#{pluralize(@poem.errors.count, "error")} prohibited this poem from being saved:"
      %ul
        - @poem.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.text_field :title, placeholder: "Título"
  .field
    = f.text_area :content, placeholder: "Poesia"
  .actions
    = f.submit 'Salvar'