.row
  .small-6.push-3.columns
    %h2.futura.center Próximas Exibições
    = link_to new_cinema_path(@cinema) do 
      .tiny.button.uppercase.radius.secondary +
      Adicionar Data de Exibição
    - @cinemas.each do |cinema|
      .panel.large-centered
        %h6
          %span.typewriter
            = cinema.place
          %br
          %span.times.destaq1
            = cinema.date
          %br
          %small
            = cinema.address

        - if current_admin
          %small
            = link_to 'Visualizar', cinema, class: "small button secondary round"
            = link_to 'Editar', edit_cinema_path(cinema), class: "small button secondary round"
            = link_to 'Deletar', cinema, method: :delete, data: { confirm: 'Tem certeza?' }, class: "small button alert round"